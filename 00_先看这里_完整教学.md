# Supplier Database Agent：完全新手使用教学

这套项目的最终目标是：

> 以后你只需要在 Codex 中输入一个公司名，例如“整理 Brose 的全球生产基地和研发中心”，Codex 就使用固定 Agent 工作流完成研究、匹配、地区 ID、新增供应商、LanguageType、QA 和 Excel 导出。

你不需要学习 Git 命令，也不需要自己写 Python/JavaScript。

---

# 一、你最终在电脑上只保留一个长期项目文件夹

推荐位置（Windows）：

`C:\Users\你的Windows用户名\Documents\Codex\supplier-database-agent\`

例如如果 Windows 用户名是 `Rebecca`：

`C:\Users\Rebecca\Documents\Codex\supplier-database-agent\`

以后不要再建立：

- supplier-database-agent-v2.2
- supplier-database-agent-v2.3-final
- supplier-database-agent-new

长期只维护：

`supplier-database-agent`

GitHub 用于保存规则、Skill、脚本和测试；五个真实数据库/reference 文件一直留在你的电脑本地。

---

# 二、第一步：下载并解压本压缩包

1. 下载压缩包。
2. 在 Windows 文件资源管理器打开：
   `文档 (Documents)`
3. 如果没有 `Codex` 文件夹，新建：
   `Codex`
4. 把压缩包里的整个项目文件夹解压到这里。
5. 最终把文件夹名称改为：
   `supplier-database-agent`

最终位置应类似：

`C:\Users\你的用户名\Documents\Codex\supplier-database-agent\`

---

# 三、第二步：把 5 个真实数据库文件放入 references

打开：

`C:\Users\你的用户名\Documents\Codex\supplier-database-agent\references\`

必须放入以下 5 个文件，并尽量使用以下固定文件名：

1. `supplier_master.xlsx`
   - 当前供应商数据库名称库
   - 至少包含库中公司/工厂名称、主营产品、Country/Province/City 数字 ID

2. `region_master.csv`
   - 地区主表
   - `id / level / parentid / areacode / internalprimarykey` 等用于地区匹配

3. `productionbase_import_template.xlsx`
   - 生产基地正式导入模板，或历史成功导入示例

4. `rndcenter_import_template.xlsx`
   - 研发中心正式导入模板，或历史成功导入示例

5. `new_supplier_import_template.xlsx`
   - 新增供应商正式导入模板，或历史成功导入示例

如果你有历史上成功新增过“额外工厂节点”或“研发中心节点”的 Excel，也放在 references 中。它主要用于确认 PLANT_SITE / RND_SITE 的 DataType。

## 非常重要

这些文件：
- 必须存在于本机 references 文件夹
- Codex 运行时必须读取
- 但 `.gitignore` 会阻止它们上传 GitHub

“GitHub 没有”不等于“Codex 不能使用”。Codex 打开的是你本机项目目录，所以照样能够读取它们。

---

# 四、第三步：检查本地文件夹结构

最终至少应是：

```text
supplier-database-agent/
├─ AGENTS.md
├─ VERSION
├─ CHANGELOG.md
├─ .gitignore
├─ skills/
│  ├─ research-skill/
│  ├─ database-enrichment/
│  └─ new-supplier-enrichment/
├─ rules/
├─ config/
├─ references/
│  ├─ supplier_master.xlsx
│  ├─ region_master.csv
│  ├─ productionbase_import_template.xlsx
│  ├─ rndcenter_import_template.xlsx
│  └─ new_supplier_import_template.xlsx
├─ work/
├─ cache/
└─ output/
```

第一次 Codex 初始化后，它可以继续完善 `scripts/`、`tests/` 和实际字段映射。

---

# 五、第四步：创建 GitHub 私有仓库

1. 打开 GitHub。
2. 登录账号。
3. 右上角点 `+`。
4. 选择 `New repository`。
5. Repository name 填：
   `supplier-database-agent`
6. 选择：
   `Private`
7. 不要勾选：
   - Add a README
   - Add .gitignore
   - Choose a license
8. 点击 `Create repository`。
9. 创建后复制仓库地址，例如：
   `https://github.com/你的用户名/supplier-database-agent`

先不要自己输入 Git 命令。

---

# 六、第五步：让 Codex 打开本机整个项目

在 Codex 中：

1. 选择打开文件夹 / Open Folder。
2. 选择：
   `C:\Users\你的用户名\Documents\Codex\supplier-database-agent`
3. 不要只打开某一个 SKILL.md。
4. 必须让 Codex 看到整个项目目录。

---

# 七、第六步：第一次给 Codex 发“项目初始化 + GitHub”提示词

打开本项目根目录：

`01_给Codex_第一次初始化并上传GitHub.txt`

把其中 `【你的GitHub仓库地址】` 替换成刚刚创建的仓库地址。

整段复制给 Codex。

本轮的目的不是研究公司，而是一次性把工具初始化好。

Codex 应该完成：
- 读取所有 Skill / rule
- 检查 5 个本地 reference 文件
- 识别真实模板字段
- 完成通用脚本和测试
- 建立缓存/分阶段执行
- 检查 DataType
- 初始化 Git
- 提交代码和规则
- 推送到 GitHub

五个真实主数据文件仍留在本地，不上传。

---

# 八、第一次初始化成功的判断标准

Codex 最终至少应该告诉你：

- 5 个主文件是否识别成功
- supplier_master 实际字段
- region_master 实际字段与 ID 规则
- productionbase 模板实际字段
- rndcenter 模板实际字段
- new supplier 模板实际字段
- LEGAL_ENTITY / PLANT_SITE / RND_SITE 的 DataType 是否确认
- 测试是否通过
- GitHub 仓库是否推送成功
- 当前版本号

如果只有 PLANT_SITE / RND_SITE DataType 未确认，不要让 Codex猜。你需要再提供历史成功样例或数据库规则。

---

# 九、以后第一次真正整理公司

初始化通过后，你再开始公司任务。

打开：

`02_给Codex_日常整理公司.txt`

把：

`【公司名称】`

换成实际公司，例如：

`Brose`

整段发送即可。

---

# 十、以后最简单的日常用法

如果整个项目已经稳定，以后甚至只需要说：

`整理 Hyundai Mobis Co., Ltd. 的全球生产基地和研发中心，按当前 supplier database agent 工作流生成全部正式导入文件和 Review。`

Codex 应自动完成：

```text
组合研究 Skill
→ research.json
→ 法人/Site标准化
→ 地区ID
→ supplier master匹配
→ 新增记录识别
→ 只对 NEW 记录补基础信息
→ LanguageType 0/1/8
→ QA
→ Excel
```

---

# 十一、输出文件在哪里

所有公司结果放到本机：

`C:\Users\你的用户名\Documents\Codex\supplier-database-agent\output\`

例如：

```text
01_Brose_ProductionBase_Import.xlsx
02_Brose_RDCenter_Import.xlsx
03_Brose_NewSupplier_Import.xlsx
04_Brose_Review.xlsx
```

`output/` 不提交 GitHub。

---

# 十二、以后更新 Agent 规则怎么办

不要重新上传完整 Skill，不要重新下载 ZIP。

直接在当前 Codex 项目对话中说你的新规则。

然后使用：

`03_给Codex_更新规则.txt`

作为固定提示词框架。

Codex 应：
- 修改最合适的规则文件
- 不复制规则到很多地方
- 增加/更新测试
- 更新 CHANGELOG
- 提升 VERSION
- 运行测试
- commit
- push GitHub

---

# 十三、例：你新增一条规则

你只需说：

`新增规则：如果额外工厂不是独立法人，新增 PLANT_SITE 时 CRN、TIN、注册资本和 CEO 不得继承主体。`

再要求 Codex：

`按项目 Rule Update Protocol 更新规则、回归测试、CHANGELOG 和 VERSION，测试通过后提交并推送 GitHub。不要修改无关文件。`

不需要重新上传任何文档。

---

# 十四、以后 supplier_master 更新怎么办

你的数据库会持续变化，所以 supplier_master 也会更新。

操作方法非常简单：

1. 从数据库导出最新供应商表。
2. 命名为：
   `supplier_master.xlsx`
3. 打开本地：
   `supplier-database-agent\references\`
4. 用新文件覆盖旧文件。
5. 给 Codex 发：

`supplier_master.xlsx 已更新。请检测主数据变化，清理/重建仅与 supplier master 有关的缓存和索引，不要重新研究已有公司，不要修改规则。`

GitHub 不需要更新 supplier_master 文件。

---

# 十五、region_master 更新怎么办

同理：

1. 新地区表改名：`region_master.csv`
2. 覆盖本地 references 中旧文件。
3. 给 Codex 发：

`region_master.csv 已更新。请检测变化，重建地区索引和相关缓存；不要重新研究公司，不要修改业务规则。`

---

# 十六、导入模板更新怎么办

如果数据库模板字段发生变化：

1. 把新的模板覆盖本地 references 中对应文件。
2. 给 Codex 发：

`数据库导入模板已更新。请重新识别模板 Sheet、字段、类型和字段映射；只修改模板映射/导出层及相关测试，不要修改研究规则和实体匹配规则。`

---

# 十七、GitHub 到底保存什么

应该上传 GitHub：
- AGENTS.md
- skills/
- rules/
- schemas/
- scripts/
- tests/
- config/local.example.json
- README / PROJECT_MAP
- CHANGELOG.md
- VERSION
- .gitignore

不上传 GitHub：
- supplier_master.xlsx
- region_master.csv
- 三个真实 Excel 模板
- output/
- work/
- cache/
- node_modules/
- config/local.json

---

# 十八、如果换电脑怎么办

1. 在新电脑安装/登录 GitHub 和 Codex。
2. Clone `supplier-database-agent` 私有仓库。
3. 在项目 `references/` 中重新放入 5 个真实 reference 文件。
4. 如需要，把 `config/local.example.json` 复制成 `config/local.json`。
5. 给 Codex 发一次项目检查提示词。

规则、Skill、程序不需要重新上传，因为已经在 GitHub。

---

# 十九、如果 Codex 又开始为单家公司写脚本怎么办

如果你看到：

- build_brose_research.mjs
- build_opmobility_research.mjs
- hyundai_matcher.py
- zf_rules.js

立刻告诉 Codex：

`停止创建公司专用程序。公司事实必须写入 work/<company>/research.json；通用能力必须写入共享模块。请按 AGENTS.md 重构本次修改。`

---

# 二十、最关键的日常原则

你只需要记住 4 件事：

1. 项目长期只有一个本地文件夹：`supplier-database-agent`
2. 5 个真实数据库文件一直放本地 `references/`
3. GitHub 保存 Agent，不保存运行主数据
4. 新规则直接告诉 Codex，让它修改仓库并提交，不再重新上传整套文档
