# Supplier Database Agent — 新用户安装与使用

本项目用于自动整理汽车供应商的全球生产基地和研发中心，并生成数据库导入文件。

## 一、你需要准备什么

### 1. Codex

需要能够使用 Codex 打开本地项目目录。

### 2. 本项目代码

从 GitHub 克隆或下载：

```text
supplier-database-agent
```

建议放置路径：

```text
C:\Users\<你的用户名>\Documents\Codex\supplier-database-agent
```

### 3. 本地数据库文件

以下真实数据库文件不会包含在 GitHub 仓库中。

请将你有权限使用的文件放入：

```text
supplier-database-agent\references\
```

文件名：

```text
supplier_master.xlsx
region_master.csv
productionbase_import_template.xlsx
rndcenter_import_template.xlsx
new_supplier_import_template.xlsx
```

这些文件是 Agent 正常运行所必需的。

---

# 二、目录应该长这样

```text
supplier-database-agent/
├── AGENTS.md
├── README.md
├── CHANGELOG.md
├── VERSION
├── skills/
├── rules/
├── scripts/
├── tests/
├── schemas/
├── references/
│   ├── supplier_master.xlsx
│   ├── region_master.csv
│   ├── productionbase_import_template.xlsx
│   ├── rndcenter_import_template.xlsx
│   └── new_supplier_import_template.xlsx
├── work/
├── cache/
└── output/
```

其中：

```text
skills/
rules/
scripts/
tests/
schemas/
```

来自 GitHub。

```text
references/
```

中的真实数据库文件由每个使用者自己提供。

---

# 三、第一次使用

使用 Codex 打开整个：

```text
supplier-database-agent
```

文件夹。

不要只打开单独的 `SKILL.md`。

第一次向 Codex 发送：

> 请阅读项目根目录 AGENTS.md、所有 skills、rules、schemas 和 README，并检查 references 中的 supplier_master.xlsx、region_master.csv 和三个导入模板。  
>  
> 请完成本机初始化：  
> 1. 验证所有运行必需文件存在；  
> 2. 检查真实字段结构；  
> 3. 建立或刷新 Supplier Master 与 Region Master 索引缓存；  
> 4. 验证 Excel 模板映射；  
> 5. 运行全部回归测试；  
> 6. 不研究任何公司；  
> 7. 不修改 references 中的原始文件。  
>  
> 最后告诉我是否已经可以开始运行公司任务，以及仍缺少哪些必须文件。

---

# 四、以后正常使用

例如整理 Brose：

> 整理 Brose 的全球生产基地和研发中心，并按当前 supplier database agent 工作流生成全部正式数据库导入文件和 Review。

Agent 应自动完成：

```text
公司研究
→ 法人和Site确认
→ 地区ID匹配
→ Supplier Master匹配
→ 新增供应商识别
→ 新供应商基础信息补全
→ LanguageType 0/1/8
→ QA
→ Excel导出
```

最终文件位于：

```text
output/
```

---

# 五、主数据更新

如果你更新了：

```text
supplier_master.xlsx
region_master.csv
```

或任一 Excel 模板，只覆盖本地：

```text
references/
```

中的同名文件。

然后告诉 Codex：

> 本地 references 中的主数据文件已经更新。请检测变更，只重建相关索引、缓存或模板映射。不要重新研究公司，不要修改无关规则。

---

# 六、不要做什么

不要：

- 把真实 supplier_master 上传到公开 GitHub；
- 修改 rules 里的业务规则来解决单家公司问题；
- 为每家公司建立 `build_xxx_research.mjs`；
- 把公司事实硬编码进程序；
- 让额外工厂错误匹配到主体公司；
- 手工猜 Country / Province / City ID。

公司研究数据应进入：

```text
work/<Company>/research.json
```

而不是程序源码。

---

# 七、如果有疑难记录

不要让一条疑难记录阻塞整个公司任务。

无法确定的记录应进入：

```text
Review
```

已确认数据继续导出。